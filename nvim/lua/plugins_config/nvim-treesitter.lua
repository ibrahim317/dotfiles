require("nvim-treesitter").setup({
	highlight = {
		enable = false, -- Disable highlighting
	},
	indent = {
		enable = false, -- Disable indentation
	},
	-- You can disable other features similarly
})
